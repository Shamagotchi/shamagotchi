import React from 'react';

const intro = () => {
    const image = require("./img/front.png")
    return (
        <div className="wrapper" style={{backgroundImage:image}}>
            
        </div>
    );
};

export default intro;